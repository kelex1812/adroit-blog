---
title: "OmniStudio F-3: Managed Package vs Standard Runtime; Upgrades"
slug: day-03-f3-managed-package-vs-standard-runtime-upgrades
series: omni-studio-cert
lesson: 3
excerpt: "Explain Managed Package Runtime vs. Standard Runtime, Automatic Upgrades, and Manual Intervention Upgrade. — Day 3 of the 46-requirement OmniStudio Developer exam prep."
date: "August 05, 2026"
author: "Adroit Consulting"
readTime: "6 min read"
tags: [OmniStudio, OmniStudio Fundamentals]
---
# Managed Package vs Standard Runtime; Upgrades

**Exam guide requirement:** Explain Managed Package Runtime vs. Standard Runtime, Automatic Upgrades, and Manual Intervention Upgrade.

**Domain:** OmniStudio Fundamentals (18%)

## Deep Dive

- Managed Package Runtime is the legacy Vlocity delivery model: OmniStudio components ride inside a managed package. Upgrades come as new package versions; major upgrades can require manual intervention (review, re-publish, re-test) before the org is on the new version.
- Standard Runtime is the modern model: OmniStudio components are native platform metadata delivered with Salesforce releases. They deploy via source format (Salesforce CLI) and upgrade automatically with platform releases — no package install, no manual upgrade steps.
- Automatic upgrades apply seamlessly when the platform updates; manual-intervention upgrades require human action (approve, re-publish changed components, validate customizations) before completion — this distinction is a favorite exam point.
- Runtime choice affects everything downstream: how you version (metadata in git vs package), deploy (sf project deploy vs package install), test, and what happens on release day.
- Migration between runtimes: re-deploy components as metadata, re-validate behaviors (JSON keys, action types), and verify data packs/dependencies — an org can run both during transition.

## Configuration Walkthrough

1. Identify runtime in an org: check installed packages (Vlocity/OmniStudio managed package) vs source-tracked metadata.
2. For Standard Runtime: enable source tracking, retrieve components via SF CLI, store in version control.
3. For upgrade planning: classify each upgrade as automatic or manual-intervention; schedule manual reviews.

## Exam Traps

- **Trap:** Saying Standard Runtime still ships as a managed package.
- **Trap:** Believing manual-intervention upgrades happen without human action.
- **Trap:** Mixing up 'upgrade' with 'deploy' — upgrades come from the vendor/platform; deploys are your own moves.

## Practice Questions

**Q: Which statement best describes Standard Runtime for OmniStudio components?**
- Components ship inside a managed package
- Components are native metadata that ride platform releases and deploy via source format
- Components cannot be versioned
- Upgrades always require manual intervention

**Answer: B**

Standard Runtime treats components as native metadata; upgrades ride platform releases automatically.

**Q: An upgrade 'requires manual intervention.' What does that mean?**
- The org upgrades itself with no action
- Human review/actions (re-publish, validate) are required before the upgrade completes
- The org must be rebuilt
- Only FlexCards are affected

**Answer: B**

Manual-intervention upgrades need human steps before completion; automatic ones don't.

**Q: A developer deploys OmniStudio components with `sf project deploy`. Which runtime does this imply?**
- Managed Package Runtime
- Standard Runtime
- Neither — CLI doesn't apply
- Legacy Vlocity package

**Answer: B**

Source-format CLI deployment is the Standard Runtime pattern; packages use package installs.


## Exam Tip

One axis: native metadata + automatic upgrades = Standard; package delivery + manual steps = Managed Package Runtime.


**Related requirements:** Day 5 — Implementation Tools, Day 6 — Versioning & Cloning, Day 45 — Publishing & Deployment Issues

